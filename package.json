{
  "name": "nestjs-library",
  "version": "1.0.0",
  "description": "Example of nestjs default library",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "TheStrikeM",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^8.0.5",
    "@types/node": "^16.4.10",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^3.0.2",
    "rxjs": "^7.3.0",
    "typescript": "^4.3.5"
  }
}
